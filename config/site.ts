export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Ledger UI",
  description: "Ledger test UI.",
  navItems: [
    {
      label: "Home",
      href: "/",
    },
  ]
};
